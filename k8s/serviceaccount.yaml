apiVersion: v1
kind: ServiceAccount
metadata:
  name: tech-agent-ksa
  namespace: default
  annotations:
    # Workload Identity: map this KSA to a GCP Service Account
    # Replace PROJECT_ID and GCP_SA_NAME below after running setup
    iam.gke.io/gcp-service-account: tech-agent-sa@project-e1d6b892-0092-497c-824.iam.gserviceaccount.com
